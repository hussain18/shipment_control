import NavBar from '../components/navbar';

const CustomersView = (props) => {
  return (
    <>
      <NavBar />
      <div>Hi from customers page :)</div>
    </>
  );
};

export default CustomersView;
