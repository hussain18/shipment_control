import NavBar from '../components/navbar';

const AddCustomerView = (props) => {
  return (
    <>
      <NavBar addCustomer='active' />
      <div>Hi from add customer page :)</div>
    </>
  );
};

export default AddCustomerView;
