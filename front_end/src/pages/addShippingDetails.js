import NavBar from '../components/navbar';

const AddShippingDetailsView = (props) => {
  return (
    <>
      <NavBar addShippingDetails='active' />
      <div>Hi from add Shipping Details page :)</div>
    </>
  );
};

export default AddShippingDetailsView;
